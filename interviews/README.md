# Real Interviews

One folder per company, created once an actual interview is scheduled (separate from practice — those live in `../mock-debriefs/`).

Copy `_TEMPLATE-company/` to start a new company folder, e.g. `cp -r _TEMPLATE-company "Acme AI"`.
